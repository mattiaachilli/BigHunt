package model.entities;

/**
 * 
 * This enum represents the current entity's status, it can be ALIVE, DEAD or FLOWN_AWAY(if a duck).
 *
 */

public enum EntityStatus {
    /**
     * Entity status.
     */
    DEAD, ALIVE, FLOWN_AWAY;
}
